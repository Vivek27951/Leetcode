int floorInBST(TreeNode<int> * root, int X)
{
    // Write your code here.
    int ans = 0;
    while(root){
        if(root->val>X){
            root = root->left;
        }else{
            ans = root->val;
            root = root->right;
        }
    }
    return ans;
}
